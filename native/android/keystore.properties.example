# Copy to android/keystore.properties (gitignored) — use absolute paths
storeFile=/Users/you/secrets/neon-nexus-release.keystore
storePassword=your-store-password
keyAlias=neon-nexus
keyPassword=your-key-password
