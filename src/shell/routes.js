/** Re-export for Phaser (JS) — canonical implementation in lib/shell/routes.ts */
export {
  ROUTES,
  armPlayIntent,
  destroyPhaserIfAny,
  exitToHome,
  isOnPlayRoute,
  navigateToPlay,
  saveRunAndLeavePlay,
  shellBackHref,
} from '../../lib/shell/routes.ts';
